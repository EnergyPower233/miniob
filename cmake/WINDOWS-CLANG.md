# Windows Clang

See BUILDING.md in this directory for the current cross-platform presets,
VS Code tasks, dependency setup, output directories, and known limitations.

The legacy windows-clang-debug and windows-clang-release presets are retained.
The unified debug/release presets are now used by VS Code on all hosts.
